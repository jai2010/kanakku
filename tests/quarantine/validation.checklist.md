# Quarantined: former `tests/validation.test.ts`

Removed from the Jest graph in H1 because it was tautological kernel evidence
(`expect(true).toBe(true)` plus a printed checklist). It must not count as a
passing suite.

This file is not collected by `testMatch: '**/*.test.ts'`.
